You are working in an Orca worktree of github.com/myzona/voicepe-realtime (fork of TristanBrotherton/voicepe-realtime), branch `live1-port`. Read `docs/LIVE1-PORT-PLAN.md`, `docs/LIVE1-PHASE1-REPORT.md` (especially "Decisions" and "Phase 2 notes") and `docs/LIVE1-PHASE1-BRIEF.md` first and follow them — do NOT invent features beyond the plan.

STATE: phase 1 is DONE and live. The add-on `openai_realtime_voice_agent/` (v0.17.0-live.1) runs on pipecat-ai 1.10.0 and is deployed on the office Voice PE as `local_openai_realtime_voice_agent`; regression on `gpt-realtime-2` passed. Do not regress that path.

TASK = PHASE 2 + PHASE 3 of the plan: add the GPT-Live-1 path.

Scope:
1. New add-on option `openai_model` value `gpt-live-1` (add to the model dropdown in `config.yaml` + translations; keep `openai_model_custom` escape hatch working) and a new option `live_backend_model` (string, default `gpt-5.4-mini`, hidden/optional like the other escape hatches is fine). When `gpt-live-1` is selected, `Application.create_openai_service` builds `pipecat.services.openai.live.OpenAILiveLLMService` with `ResponsesDelegation(settings=OpenAIResponsesLLMService.Settings(model=<live_backend_model>))` instead of `SafeRealtimeLLMService`. Read the installed source in the venv `/Users/edwardkhytrykh/.openclaw/tmp/opencode/live1/v110/lib/python*/site-packages/pipecat/services/openai/live/` (or pip show location) for the real constructor/Settings shape — do not guess. Verified protocol facts (2026-09-16, our key): `wss://api.openai.com/v1/live/sessions`, first message `session.start` with `{"session":{"model":"gpt-live-1","delegation":{"type":"responses","responses":{"model":"gpt-5.4-mini"}}, ...}}` → `session.started`; default voice `marin`, output audio PCM 24 kHz (pipeline already runs at 24 kHz). `delegation.model` (flat) is rejected with `missing_required_parameter session.delegation.responses`.
2. Register the SAME tool handlers for the Live service that the Realtime path registers (HA MCP tools via `HomeAssistantMCPService`, `ask_openclaw` direct tool, `recall_memory`, timers, enrollment, web_search, disconnect) with `cancel_on_interruption=False`, so HA control works identically. Persona `instructions` → the Live `session.instructions`. Voice option: Live voices differ from Realtime voices — verify the accepted list from the pipecat source / API error messages and map/validate (default `marin`); do not silently send an invalid Realtime voice name.
3. Turn handling for Live (phase 3 of the plan): Live-1 is full-duplex server-side; the plan says `ExternalUserTurnStrategies(enable_interruptions=False)` and no client VAD/turn-detection settings. Read the phase-1 report "Phase 2 notes" — prefer the same SERVICE-DRIVEN turn-frame override used for Realtime (emit UserStarted/StoppedSpeakingFrame + interruption from the service on the Live server's speech events) so the aggregator-side `UserTurnController` is bypassed, unless the Live service's event surface makes that impossible — in that case document why and what you did instead. Keep the add-on's wake-gated connect/disconnect, `PhaseEmitter` listening/thinking/replying phases, `ConnectionRecovery` (Live sessions expire ~1 h → proactive refresh before `expires_at`), wedge watchdog and the `disconnect` tool working. Subclass as `SafeLiveLLMService` mirroring `SafeRealtimeLLMService`'s overrides (benign error codes, receive-task handler, reset_conversation) where they apply.
4. Tests: extend `tests/test_pipeline_smoke.py` (or add `tests/test_pipeline_smoke_live.py`) with a no-network smoke test for the Live path that asserts the `session.start` payload shape (model gpt-live-1, delegation.type responses, responses.model from the option, instructions, voice), tool registration, topology (no RTVIProcessor), and that selecting `gpt-realtime-2` still produces the exact phase-1 payload. All tests must pass: `PYTHONPATH=. python -m pytest tests` and `python -m unittest discover -s tests`.
5. Bump version to `0.17.0-live.2` in `config.yaml`, CHANGELOG entry "GPT-Live-1 support (openai_model: gpt-live-1, live_backend_model)". Update `DOCS.md`/translations for the new options briefly.
6. Verify `docker build --platform linux/amd64 -f openai_realtime_voice_agent/Dockerfile --build-arg BUILD_FROM=ghcr.io/home-assistant/amd64-base-debian:bookworm openai_realtime_voice_agent/` still succeeds on this Mac. Do NOT build/install anything on the Home Assistant VM and do NOT touch any running add-on — the coordinator deploys.
7. Optional but valuable if cheap: a tiny standalone script `openai_realtime_voice_agent/tools/live_probe.py` that opens a Live session with the key from env `OPENAI_API_KEY` (never argv, never printed), sends `session.start`, and prints the `session.started` summary — for the coordinator to smoke-test before deploy. Do not run it yourself with any key you find on disk.

Rules: commits authored as the repo's default git user (Edward / myzona) — NEVER add Co-authored-by or any AI attribution trailers. Small, reviewable commits. Push the branch to `origin` (myzona fork): `git push origin HEAD:live1-port` (local branch may be named `myzona/live1-port`). Do not open a PR to upstream. When finished, write `docs/LIVE1-PHASE2-REPORT.md` (what changed, how the turn handling was solved, verification output, live-regression checklist for the coordinator, open risks) and commit it. Ergo is NOT reachable from this worktree — record decisions in the report only.
