You are working in an Orca worktree of github.com/myzona/voicepe-realtime (fork), branch `live1-port`. Read `docs/LIVE1-PORT-PLAN.md`, `docs/LIVE1-PHASE2-REPORT.md` and `docs/LIVE1-PHASE2-BRIEF.md` first. Do not invent features beyond fixing the problems below; keep the `gpt-realtime-2` path byte-identical (tests pin it).

STATE: add-on 0.17.0-live.2 is DEPLOYED on the office Voice PE as `local_openai_realtime_voice_agent` in GPT-Live-1 mode (voice cedar, backend gpt-5.4-mini, 40 function tools). Edward's live regression verdict: "kind of laggy". He keeps Live-1 on the puck ONLY if every problem below is fixed. Evidence: `docs/live1-lag-log-20260916.txt` (add-on log excerpt, real session 10:11–10:25 PDT; secrets masked). Do NOT touch the HA VM or any running add-on; the coordinator deploys.

TASK = PHASE 4 (fix Live-1 quality/latency) — all of these:

1. **17 s stall after a tool result (the main lag).** `web_search` turn at 10:23:38: user transcript 10:23:39.5 → function call 10:23:40.8 → add-on's Responses call returned 10:23:46.0 → `_send_function_call_output` + `_maybe_continue_response` at 10:23:46.09 → NOTHING until "Bot started speaking" 10:24:03.3 (17 s; the thinking-watchdog even forced the device idle at 15 s). HA MCP tools in the same session answered ~1 s after their result (10:23:29 → 10:23:32), so find what differs: is the continuation message wrong/missing (read pipecat `services/openai/live/llm.py` `_maybe_continue_response`, `_send_function_call_output`, delegation state machine: is there a pending-delegation counter that only continues after BOTH outputs when multiple calls are batched? did the `[hum]` filler response hold the delegation open?), does the Live service surface server events (delegation.completed / response.* / errors) that we are not logging, is the output silence gate (`_dropped_silence_frames`) swallowing the start of the answer? Add INFO-level logging of every Live server event type with timestamps (rate-limited for audio) so this is diagnosable from the add-on log, then fix the root cause. Also reconsider the add-on's own `web_search` implementation on Live: the phase-2 report risk #8 says the Live backend can use the built-in Responses `{"type":"web_search"}` tool directly — if that removes a 5 s hop and the stall, do it for `gpt-live-1` mode (keep the client tool for Realtime).
2. **Wrong time.** "what time is it" at 10:23:08 PDT → "It's 4:06 p.m." with NO `llm__GetDateTime` call (Realtime called the tool). Make the backend call the time tool: strengthen the backend prompt preamble (backend has its own prompt per phase-2 report risk #5 — give it explicit tool-use rules: always call `llm__GetDateTime` for time/date, never guess; call HA tools for device state; answer only after results) and/or set the Responses `tool_choice`/reasoning options that pipecat exposes. Verify with a no-network test that the preamble contains the rule and the payload carries any option you set.
3. **"[hum]" filler + phase flapping.** Every reply starts with an assistant transcript "[hum]" (the model vocalises a filler while delegating) and phases flap `replying → listening → thinking` on transcript fragments (10:23:05–08, 10:23:38–39). Find the Live session setting that controls filler/backchannel vocalisation (read the pipecat Live settings model and the `session.start` schema for anything like `output_audio`/`speech`/`filler`/`acknowledgement`/`thinking_sound`; if none exists, instruct against it in `session.instructions` and measure) and stop the phase flapping (a transcript fragment arriving while the bot is speaking must not flip to `listening`; debounce or gate on `BotStopped`).
4. **Extra latency per turn.** Live adds a delegation hop (~1–2 s before `replying`). Measure from the log and report per-turn timings (user transcript end → first audio) for Live vs the Realtime numbers in `docs/live1-lag-log-20260916.txt` / phase-1 notes; if a faster backend model is allowed by the API (e.g. `gpt-5.4-nano` or a `-mini` with `reasoning: {effort: minimal}` / `text.verbosity: low`), expose the knob (option `live_backend_model` already exists; add `live_backend_reasoning_effort` if the Settings support it) and recommend defaults.
5. Tests stay green (`pytest` + `unittest discover`; extend the Live smoke test for each fix), bump to `0.17.0-live.3` + CHANGELOG, `docker build --platform linux/amd64 ...` must pass. Push to `origin HEAD:live1-port`. Write `docs/LIVE1-PHASE4-REPORT.md` with root cause of the stall, each fix, per-turn latency table, and what the coordinator should re-test (short list).

Rules: commits authored as Edward/myzona, NEVER add Co-authored-by or AI trailers. No PR to upstream. Never run anything with an API key you find on disk; the coordinator runs live probes.
